{
  "name": "blink-claw-skill",
  "version": "1.0.0",
  "private": true,
  "description": "Blink Lightning wallet CLI scripts for OpenClaw skill system",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "lint": "eslint blink/scripts/",
    "lint:fix": "eslint --fix blink/scripts/",
    "format": "prettier --write blink/scripts/",
    "format:check": "prettier --check blink/scripts/"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
